---
import { getArticles } from '@/utils/articles'

export async function getStaticPaths() {
  return (await getArticles()).map(post => ({ params: { id: post.id }, props: { id: post.id } }))
}
return Astro.redirect(encodeURI(`/blog/${Astro.props.id}`), 301)
---
