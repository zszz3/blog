---
import { getBlogCollection } from 'astro-pure/server'
export async function getStaticPaths() {
  return (await getBlogCollection()).map(post => ({ params: { id: post.id }, props: { id: post.id } }))
}
return Astro.redirect(encodeURI(`/blog/${Astro.props.id}`), 301)
---
